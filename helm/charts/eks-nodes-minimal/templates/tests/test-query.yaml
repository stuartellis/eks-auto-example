apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eks-nodes-minimal.fullname" . }}-test-query"
  labels:
    {{- include "eks-nodes-minimal.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: eks-nodes-minimal-test-query
      image: busybox
      command: ['echo']
      args: ['$EXAMPLE1']
      env:
        - name: EXAMPLE1
          valueFrom:
            configMapKeyRef:
              name: {{ include "eks-nodes-minimal.fullname" . }}
              key: example1
  restartPolicy: Never
