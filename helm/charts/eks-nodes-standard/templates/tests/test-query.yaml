apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eks-nodes-standard.fullname" . }}-test-query"
  labels:
    {{- include "eks-nodes-standard.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: eks-nodes-standard-test-query
      image: busybox
      command: ['echo']
      args: ['$EXAMPLE1']
      env:
        - name: EXAMPLE1
          valueFrom:
            configMapKeyRef:
              name: {{ include "eks-nodes-standard.fullname" . }}
              key: example1
  restartPolicy: Never
